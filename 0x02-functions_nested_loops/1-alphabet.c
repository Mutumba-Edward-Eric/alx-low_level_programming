#include "main.h"

/**
 * main - check the code
 * print_alphabet - Print the alphabet in lowercase.
 * Always 0.
 */

int print_alphabet(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)

	{
		_putchar(ch);
	}
	_putchar('\n');
	return (0);
}
